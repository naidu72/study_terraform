# Terraform state — never commit these
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
backends/
# Sensitive var files
*.tfvars
!example.tfvars    # allow the example template only

# Crash logs
crash.log
crash.*.log
